import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CustomerGroups } from './customer-groups';
describe('CustomerGroups', () => { it('should create', () => { expect(true).toBe(true); }); });